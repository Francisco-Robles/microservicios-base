server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: zipkin
          uri: https://github.com/Francisco-Robles/microservicios-base
          search-paths: config-data
  application:
    name: config-server
